#include <bits/stdc++.h>
using namespace std;
int main() {
  double A = 0, B = 0, C = 0;
  cin >> A >> B >> C;
  cout << "MEDIA = " << fixed << setprecision(1) << (A * 2 + B * 3 + C * 5) / 10 << endl;
  return 0;
}
